import {expect} from "chai";

describe("Formula", () => {
    it("placeholder", () => {
        const expected = null;
        const actual = null;
        expect(actual).to.equal(expected);
    });
});